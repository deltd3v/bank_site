import { Component } from 'solid-js';
import styles from './styles.module.scss';

export const HeroSection: Component = (props) => {
	return (
		<div id="hero" class={styles['HeroContainer']}>
			hero
		</div>
	);
};

export default HeroSection;
