import { Component } from 'solid-js';
import styles from './styles.module.scss';

export const C: Component = (props) => {
	return (
		<div id="C" class={styles['CContainer']}>
			C
		</div>
	);
};

export default C;
