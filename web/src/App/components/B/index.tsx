import { Component } from 'solid-js';
import styles from './styles.module.scss';

export const B: Component = (props) => {
	return (
		<div id="B" class={styles['BContainer']}>
			B
		</div>
	);
};

export default B;
